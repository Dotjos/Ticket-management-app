<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{% block title %}Ticketrax{% endblock %}</title>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'deep-blue': '#001F3F',
              'blue-900': '#1E3A8A',
            },
          },
        },
      };
    </script>


    <!-- Toastify CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
  </head>

  <body>
    <div class="min-h-screen flex items-center justify-center bg-gradient-to-b from-blue-50 to-blue-100 relative overflow-hidden">

      <!-- Decorative Circles -->
      <div class="absolute -top-10 -left-10 w-40 h-40 bg-deep-blue rounded-full opacity-10"></div>
      <div class="absolute bottom-0 right-0 w-56 h-56 bg-blue-300 rounded-full opacity-20"></div>

      <!-- Wave Background -->
      <svg
        class="absolute bottom-0 left-0 w-full"
        viewBox="0 0 1440 320"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          fill="#1E40AF"
          fill-opacity="0.3"
          d="M0,224L48,213.3C96,203,192,181,288,154.7C384,128,480,96,576,117.3C672,139,768,213,864,240C960,267,1056,245,1152,202.7C1248,160,1344,96,1392,64L1440,32V320H0Z"
        />
      </svg>

      <!-- Auth Card -->
      <div class="relative z-10 w-[90%] max-w-md bg-white/80 backdrop-blur-md shadow-lg rounded-2xl p-8">
        <h1 class="text-2xl font-bold text-center text-deep-blue mb-6">
          Ticketrax
        </h1>

        <!-- Outlet -->
        {% block content %}{% endblock %}
      </div>

    </div>

        {% block scripts %}{% endblock %}
  </body>
</html>
